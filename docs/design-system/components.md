# Components

Current primitives include buttons, cards, surfaces, inputs, textarea, select, checkbox, progress bar, progress ring, badges, stars, level badge, feedback panels, hint panel, empty/error states, skeleton, lesson block shell and question option states.

Components use typed props and semantic elements. Student-facing primary actions should normally use large buttons.
